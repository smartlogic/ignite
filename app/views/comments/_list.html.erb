<h3>Comments</h3>
<% @comments.each do |comment| -%>
  <p>
    <strong><%= link_to_if comment.url != "http://", h(comment.author), comment.url, :class => "author" %></strong>
    <span class="timestamp"><%= pp_date comment.created_at %></span><br />
    <%= Markdown.new(comment.content).to_html %>
  </p>
<% end -%>