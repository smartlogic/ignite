<h1>New sponsor</h1>

<% form_for @sponsor, :url => admin_sponsors_path, :html => {:multipart => true}, :method => :post do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', admin_sponsors_path %>
