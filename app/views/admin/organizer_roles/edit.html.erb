<h1>Editing organizer_role</h1>

<% form_for([:admin, @organizer_role]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', [:admin, @organizer_role] %> |
<%= link_to 'Back', admin_organizer_roles_path %>
