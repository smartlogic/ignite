<h1>New Event</h1>

<% form_for([:admin, @event], :html => {:multipart => true}) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "event_fields", :locals => {:f => f, :event => @event} %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', admin_events_path %>